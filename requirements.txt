# Core Azure dependencies
azure-identity>=1.15.0
azure-storage-blob>=12.19.0
openai>=1.12.0

# Vector store and embeddings
chromadb>=0.4.22

# Financial data
yfinance>=0.2.36
requests>=2.31.0  # For Polygon.io fallback
lxml>=5.0.0  # Required by yfinance for parsing HTML (earnings dates, etc.)

# CLI and utilities
typer>=0.9.0
rich>=13.7.0  # For beautiful CLI output

# Data processing
pandas>=2.2.0
numpy>=1.26.0

# Configuration
python-dotenv>=1.0.0
pydantic>=2.6.0
pydantic-settings>=2.1.0

# Logging and monitoring
structlog>=24.1.0

# Social Media APIs (Phase 3)
tweepy>=4.14.0  # X (Twitter) API
praw>=7.7.1  # Reddit API
textblob>=0.17.1  # Simple sentiment analysis
xpoz>=0.1.0  # xpoz.ai unified social media API (Twitter, Reddit, TikTok, Instagram)

# Telegram Bot (Phase 3)
python-telegram-bot>=20.7  # Telegram bot framework

# Machine Learning (Phase 4)
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
joblib>=1.3.0
optuna>=3.0.0

# Azure ML (Optional - only for distributed training on Azure ML)
# If you want to train on Azure ML, install separately:
#   pip install -r ml/azure_ml/requirements-azure-ml.txt
# For local training, these are not required

# Testing (optional for Phase 1, but good to have)
pytest>=8.0.0
pytest-asyncio>=0.23.0

# UI
fastapi>=0.109.0
uvicorn>=0.27.0
gunicorn>=21.2.0  # Production WSGI server for Azure App Service

# Authorization
permit>=2.4.0  # Permit.io SDK for authorization

# Authentication
pyjwt>=2.8.0  # JWT tokens for session management
python-jose[cryptography]>=3.3.0  # JWT with cryptographic signing
passlib>=1.7.4  # Password hashing utilities

# Email
resend>=0.8.0  # Modern email API (recommended)
azure-communication-email>=1.0.0  # Azure Communication Services Email
# Alternative: sendgrid>=6.11.0  # SendGrid email service
# Alternative: python-dotenv for SMTP configuration
